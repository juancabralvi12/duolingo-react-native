Read AGENTS.md first and follow it strictly.

Implement the full design system using NativeWind based on the attached design theme. Set up global CSS utilities, separate design tokens in the theme folder, and font loading/config for the app. Ensure everything matches the attached design theme.

Required delta for Vocalingo:

- The design system should support a playful AI vocal training app.
- Name examples around vocal tracks, singing lessons, coach feedback, XP, streaks, and practice drills.
- Preserve the existing visual quality bar and avoid adding new libraries.

@prompt_material/01-design-system.png
