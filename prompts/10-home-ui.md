Read AGENTS.md first and follow it strictly.

Implement the Home screen UI exactly as shown in the attached design with the same spacing and structure. Display the logged-in user information from Clerk and the selected vocal track from Zustand + AsyncStorage.

Use the learning data from `data/*` to show current vocal lesson, progress, and today's plan.

Use assets from the assets folder via the centralized images import. If any image is missing, use a suitable placeholder from Unsplash or Picsum.

Required delta for Vocalingo:

- Replace language copy with vocal training copy.
- Today's plan should include lesson work, an AI coach session, and vocal drills.
- "Continue learning" may become "Continue training"; progress and XP stay local mocks unless a later prompt changes them.

@prompt_material/05-home-and-tab-navigation.png
